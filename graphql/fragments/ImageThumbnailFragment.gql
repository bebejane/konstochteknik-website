fragment ImageThumbnailFragment on FileField {
	id
	mimeType
	url
	title
	alt
	height
	width
	responsiveImage(imgixParams: { q: 80, h: 400, auto: format, sharp: 80 }) {
		src
		width
		height
		alt
		title
		base64
	}
}
